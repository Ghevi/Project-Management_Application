package com.ghevi.utils.services;

import org.springframework.stereotype.Service;

@Service // @Service is a mark for services, also can use @Component (mark if class is component, catch all) or @Repository (mark if class is for data)
public class DataCleansingService {

    public DataCleansingService(){
        super();
    }
}
